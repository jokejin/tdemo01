name: Main CI/CD Workflow
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs: 
  # 1. Call the Gitleaks workflow
  run-gitleaks:
    uses: ./.github/workflows/gitleaks.yml

  # 2. Call the SonarQube workflow
  run-sonarqube:
    uses: ./.github/workflows/sonarq.yml
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # 3. Handle Slack Notifications for Success AND Failure
  notify-slack:
    needs: [run-sonarqube, run-gitleaks] 
    
    # 'always()' ensures this job runs even if Gitleaks or SonarQube fail
    if: always() 
    uses: ./.github/workflows/notify_slack.yml
    with:
      gitleaks_result: ${{ needs.run-gitleaks.result }}
      sonarqube_result: ${{ needs.run-sonarqube.result }}
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
